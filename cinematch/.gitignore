# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv
venv/
ENV/
env/
env.bak/
venv.bak/

# Environment variables
.env
.env.local

# UV lock file

# Data cache (except for the vector DB)
data_cache/

# Ignore everything inside data_cache EXCEPT the folder itself if we want to track specific subfolders
# data_cache/*
# !data_cache/.gitkeep

# We DO NOT ignore .endee because we want to push the Vector DB to Hugging Face
# !.endee/
